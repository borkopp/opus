# ─────────────────────────────────────────────────────────────────────────────
# Runtime secrets for opus-mk container
# Copy this to /opt/opus/.env.mk on the VPS and fill in real values.
# opus-mk has no server-side secrets — all public vars are baked into the image.
# Add anything here only if you introduce server-side logic later.
# ─────────────────────────────────────────────────────────────────────────────

# Currently empty — NEXT_PUBLIC_* vars are inlined at build time via GitHub secrets
