# Expose only the local Polar webhook, never Convex auth or other HTTP routes.
on_http_request:
  - expressions:
      - "req.url.path != '/webhooks/polar' || req.method != 'POST'"
    actions:
      - type: custom-response
        config:
          status_code: 404
          content: Not found
