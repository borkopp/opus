tunnel: d2f8c693-470d-4ffe-9bc3-59143d7f16c4
credentials-file: /etc/cloudflared/credentials.json

ingress:
  # opus-mk marketplace
  - hostname: app.opus.mk
    service: http://127.0.0.1:3007

  # opus-dashboard
  - hostname: dashboard.opus.mk
    service: http://127.0.0.1:3006

  # Self-hosted Convex backend (queries / mutations / websockets)
  - hostname: api.opus.mk
    service: http://127.0.0.1:3210

  # Self-hosted Convex HTTP actions
  - hostname: sites.opus.mk
    service: http://127.0.0.1:3211

  # Catch-all
  - service: http_status:404
